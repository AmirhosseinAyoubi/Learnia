[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP"]
ignore = ["E501"]   # long lines acceptable in CSS strings

[tool.ruff.lint.per-file-ignores]
"screens/*.py" = ["UP007"]   # allow X | Y union syntax with older textual generics
